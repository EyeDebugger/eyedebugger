# helpers/dotnet

The C# side helper `eyedbg-dotnet-helper`: ClrMD, `DiagnosticsClient`/EventPipe
and dump analysis, driven by the daemon for `eyedbg dotnet …` (docs/DESIGN.md
§7-8). Speaks JSON-RPC over stdio.

Planned for phase 2; no code yet.
