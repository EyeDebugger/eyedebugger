# Install the debug adapters

eyedbg drives a debug adapter per language. Install the ones you need once, from a terminal:

```
eyedbg adapters install debugpy      # Python
eyedbg adapters install netcoredbg   # .NET
```

`eyedbg adapters ls` lists every language and whether its adapter is installed.
