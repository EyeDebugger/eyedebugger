# Configuration for the DCO GitHub App (https://github.com/apps/dco).
# Lets a contributor fix a missing sign-off with a follow-up commit instead of a force-push.
allowRemediationCommits:
  individual: true
